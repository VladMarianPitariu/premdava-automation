# Inventory

Add the mapping used by remediation jobs:

```text
endpoint -> service -> VM -> Hyper-V host -> allowed action
```

Do not place passwords, gMSA secrets or private keys in this directory.
